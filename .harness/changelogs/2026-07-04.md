# 2026-07-04

## Cycle 7 — v3.5 audit cycle-6 (P0 trio fix from sub-agent audit)

**Status**: All 4 fixes landed + 7 assertions added. **209/209 pass** (up
from 196). File size 553.9 KB (unchanged, fixes too small to move the
needle — under 600 KB budget).

### Trigger
Spawned a `general` sub-agent (`mvs_46733162d13443e8b374631d5eefcc97`)
to run read-only audits on 3 critical zones the in-context review
couldn't easily probe. Agent returned 8 findings (3×P0, 4×P1, 1×P2),
all code-grounded. Cycle 7 ships the **3×P0 + 1×P1 (F-05)**. The 4×P1
findings (F-06 clock skew, F-07 visual-type ladder, F-08 picker
refresh, F-03 rect cache) are deferred to cycle 8 pending user
decision on the F-07 visual-type handling question (see Open
questions below).

### Fixes landed

| ID | Severity | Fix | File / Line |
|---|---|---|---|
| **F-01** | P0 | `Game.handleAnswer` re-entrancy null-out: `Game.state.current[team] = null` immediately before the `setTimeout(..., 0)` block. SEN-student double-click / touch bounce would otherwise hit `damageBoss` + `Adaptive.onCorrect` twice within one render cycle. Second tap early-exits at line 3742 `if (!q) return`. | `index.html` ~line 3863 |
| **F-02** | P0 | `Game.askReReview` guards `pendingUlt`: returns early when `Game.state.pendingUlt` is set. Prevents review chip tap from stomping the ult-answer question, which would cause `handleAnswer` to fire `releaseUlt` on a wrong team / wrong state. | `index.html` ~line 3874 |
| **F-04** | P0 | `Snapshot.resume` now resets `_startAt = 0` and `_frostTickAt = 0` (was previously inheriting stale values, causing the first gameLoop tick to compute `timeLeftSec = 0` → instant endGame — **time-mode resume button was effectively broken**). | `index.html` ~line 4928-4933 |
| **F-05** | P1 | `gameLoop` reordered: lazy-init `_startAt = tNow` (line 3941) now runs **before** the elapsed compute (was after a wasted first compute that used the stale `_startAt = 0`). Saves one `Math.floor` + one `Math.max` per tick and removes the first-tick display artifact. | `index.html` ~line 3938-3944 |

### Regression assertions

Added **T19 block** (7 assertions) under labels `AUDIT6-F01 / F02 / F04 / F05`:

- `AUDIT6-F01` (2) — handleAnswer nulls `current[team]` before setTimeout; comment documents the null-out rationale
- `AUDIT6-F02` (2) — askReReview early-returns when `pendingUlt` is set; comment documents the rationale
- `AUDIT6-F04` (2) — Snapshot.resume resets `_startAt` AND `_frostTickAt`
- `AUDIT6-F05` (2) — gameLoop lazy-init `_startAt` before elapsed compute; duplicate compute removed

### Result
```
node tests/regression.js → 209 pass / 0 fail
File size: 553.9 KB / 600 KB budget
```

### Open questions deferred to cycle 8

1. **F-07 visual-type Adaptive handling** — `TYPE_ORDER.indexOf('ordering') === -1`,
   so a student who nails 5 ordering questions in a row gets silently
   switched to `add10` (the easiest arithmetic type). Options:
   - (A) Skip Adaptive for visual types entirely
   - (B) Build a separate visual-type ladder
   - (C) Map visual → arithmetic equivalent (e.g. `count10` ↔ `add10`)

   This affects teacher workflow choice integrity. **Should be asked
   via AskUser before committing cycle 8.**

2. **F-08 picker refresh** — confirm that re-rendering `renderTypePicker()`
   mid-game doesn't introduce layout issues after Adaptive mutates state.

3. **F-03 rect cache** — bundle with cycle 8 (P2 perf nit).

### Next
Cycle 8 = F-06 clock-skew clamp + F-07 (after user decision) + F-08
+ F-03. ~5 assertions, file size impact negligible.