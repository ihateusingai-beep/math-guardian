# 2026-07-03

## Bootstrap — `.harness/` + root `AGENTS.md`

Established the Mavis multi-agent team for Math Guardian v3.4 + v3.5
sprint. **No code change to `index.html` or `tests/regression.js` —
189 assertions still pass unchanged.**

### Files created
- `AGENTS.md` (root, agents.md spec)
- `.harness/agent.md` (orchestrator)
- `.harness/reins/math-spa-developer/agent.md`
- `.harness/reins/regression-tester/agent.md`
- `.harness/reins/sen-a11y-reviewer/agent.md`
- `.harness/reins/tts-karaoke-specialist/agent.md`
- `.harness/docs/architecture.md`
- `.harness/docs/audit-cycle.md`
- `.harness/docs/regression-discipline.md`
- `.harness/docs/v35-roadmap.md`
- `.harness/changelogs/2026-07-03.md` (this file)

### Roster (4 reins)
- `math-spa-developer` — owns `index.html` feature work.
- `regression-tester` — owns `tests/regression.js`, 189-assertion
  lock-in discipline.
- `sen-a11y-reviewer` — owns a11y / keyboard / reduced-motion / SR.
- `tts-karaoke-specialist` — v3.5 P0 TTS Karaoke lead.

### Next sprint
v3.5 P0:
1. TTS Karaoke
2. 自動推薦
3. Question Bank lite

See `.harness/docs/v35-roadmap.md` for design sketches.

---

## Cycle 6 — v3.5 P0 TTS Karaoke (regression-assertions task)

**Status**: 2 of 12 T18 assertions fail due to contract-vs-source regex
distance mismatch (not source-level invariant failure). Reported to
parent — awaiting decision (loosen pattern / tighten implementation /
skip).

### What landed
- `tests/regression.js` — added T18 block (12 assertions across KAR-1..KAR-8)
  verbatim per `.harness/changelogs/2026-07-03-tts-karaoke-contract.md`
- No changes to T1–T17 blocks
- No changes to `index.html`
- File size budget still passing (552.5 KB < 600 KB)

### Result
`node tests/regression.js` → **199 pass / 2 fail**

Passing T18 (10/12):
- KAR-1a/b  TTS.speak accepts opts + wires onBoundary
- KAR-2     TTS.cancel clears .tts-active
- KAR-3a/b  renderQuestionTextFor emits tts-tok + reads Game.settings.ttsKaraoke
- KAR-4     showQuestion wires onBoundary through TTS.speak opts
- KAR-5a/b  teacher modal td-audio-tts-karaoke checkbox + wiring
- KAR-8a/b  aria-describedby + sr-only desc span

Failing T18 (2/12):
- **KAR-6** Snapshot.flush spreads ttsKaraoke via settings — pattern
  `/Snapshot\.flush[\s\S]{0,400}settings: \{ \.\.\.Game\.settings \}/`
  doesn't match. Source has `Snapshot.flush('beforeunload')` at
  index.html:5244 but `settings: { ...Game.settings }` is in the
  `const snap = {...}` literal at index.html:4859 (BEFORE flush is
  called). The pattern's anchor is structurally impossible.
- **KAR-7** reduced-motion disables .tts-tok transition — pattern
  `/@media \(prefers-reduced-motion: reduce\)[\s\S]{0,800}\.tts-tok[\s\S]{0,200}transition: none/`
  doesn't match. Actual distance @media → `.tts-tok, .tts-tok.tts-active`
  is **1118 chars** (over the 800 limit). Reverse-anchor pattern also
  fails (intermediate content > 400 chars). Source does have the rule
  at index.html:375–379 — just the regex distance is too tight.

### Contract-vs-source drift
Both failures are pure regex-distance issues, not missing source
invariants:
- KAR-6 source IS correct (`settings: { ...Game.settings }` in `snap`
  literal at line 4859, `Game.settings = { ...Game.settings, ...s.settings }`
  in `resume()` at line 4891)
- KAR-7 source IS correct (`.tts-tok, .tts-tok.tts-active { transition: none !important; }`
  at line 379 inside `@media (prefers-reduced-motion: reduce)` block
  starting at line 360)

Awaiting parent decision on which to update: contract spec patterns
or implement task source.
