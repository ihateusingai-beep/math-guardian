# Cycle 15 — R5 Game.handleAnswer refactor (2026-07-09)

**Status**: ship-ready
**Test delta**: 294 → 303 (+9 T28)
**File size**: 591.8 KB → 591.0 KB (char count, -134 bytes / -578 UTF-8 bytes)
**handleAnswer body**: 149 lines / 4.8 KB → 13 lines / 659 chars (orchestrator)

## Context

Cycle 14 嘅 master promote ship 後 handleAnswer 仍然係 149-line monolith —
5 個 branching concern 全部 inline 入同一個 function:

1. Triple-path answer compare (array / number / string) — 23 行
2. Pending ult branch (correct/wrong) — 25 行
3. Correct effects (energy / ult charge / 動畫 / boss damage / a11y) — 29 行
4. Wrong effects (hurt / review queue / instant hint / boss counter / a11y) — 20 行
5. Post-stats (reaction time / Profile / mastery / streak) — 13 行
6. Next-question routing (coop round-robin / versus self) — 14 行

每個 concern 都有自己嘅 invariant (F-03 ordering strict check, AUDIT6-F01
re-entrancy null-out, AUDIT7-F07 lastResumableType cache, AUDIT14 ordering
fix, etc)，inline 入 function 入面難以 unit-test / debug。R5 抽 6 個
module-level helper，handleAnswer 自己變 thin orchestrator。

## What was extracted

### `_compareAnswer(expected, actual, type)` — pure

- Returns `{ correct }`
- 3 branch: array (ordering JSON.stringify strict + length guard) / number
  (Number() coercion) / string (String() ===)
- Ordering 答錯嗰陣 console.warn 顯示期望 vs 實際（debug aid,
  production 唔 spam 因為只係 wrong path 先 fire — F-03 invariant 保留）
- 唔 touch Game.state / DOM / Audio / FX — pure function, 易 unit-test

### `_handlePendingUlt(team, q, value, correct, btn)` — boolean

- Returns `true` if ult path was taken (caller 跳過 regular stats)
- 兩個分支: correct → releaseUlt + energy/ultCharge -1 / wrong → 作罷 + 提示
- 唔變 pendingUlt 嘅 state machine，只係 resolve 嗰刻
- AUDIT6-F02 invariant (唔撞 regular path) 保留

### `_applyCorrectEffects(team, q, btn, prevRagePct)` — side-effectful

- Energy gain + ult charge random 30% + char attacking 動畫 + Audio attack/correct/energyTick
- FX.screenAlert (correct compliment) + highlightBtn(true) + damageBoss 15+random 6
- AUDIT7-F07: lastResumableType cache 保留
- AUDIT7 bossRageThreshold hp tick trigger: prevRagePct 由 caller capture
- P1-B Adaptive.onCorrect + AUDIT5 aria-live announce 保留

### `_applyWrongEffects(team, q, btn)` — side-effectful

- char hurting 動畫 + Audio wrong/reviewAdd + FX.screenAlert (wrong compliment)
- AUDIT5 aria-live announce (含正確答案)
- AUDIT4-UI2 screenShake + highlightBtn(false)
- V3.4-U2 instantHint (SEN-friendly, 跟 instantHint setting)
- Review queue push + UI.renderReview + scheduleBossCounter
- P1-B Adaptive.onWrong + P1-E-2 Profile.resetStreak

### `_recordAnswerStats(team, q, correct)` — side-effectful

- **順序重要**: recordAnswer 先 (statsByType update)，跟住 checkMastery 再讀
  accuracy (cycle 14 V3.5-AUDIT14 ordering fix 保留)
- Snapshot.schedule('answer') — P1-A
- Streak trigger (5 連勝彈鼓勵) 保留

### `_scheduleNextQuestion(team)` — side-effectful

- AUDIT6-F01 re-entrancy null-out 保留 — `current[team] = null` 喺 setTimeout fire 之前
- Coop: Game.nextTeamInCoop() + setTimeout round-robin
- Versus: setTimeout self-next

## Game.handleAnswer (final, 13 lines body)

```js
Game.handleAnswer = function(team, value, btn) {
  if (!Game.state.running || Game.state.paused || Game.state.ended) return;
  const q = Game.state.current[team];
  if (!q) return;

  const { correct } = _compareAnswer(q.answer, value, q.type);

  // Pending ult branch — return early (唔計入 regular stats)
  if (_handlePendingUlt(team, q, value, correct, btn)) return;

  // Regular answer — stats + effects + post-stats + next-question
  Game.state.teams[team].total++;
  const prevRagePct = s_prevRagePct();
  if (correct) _applyCorrectEffects(team, q, btn, prevRagePct);
  else _applyWrongEffects(team, q, btn);
  _recordAnswerStats(team, q, correct);
  UI.updateTeam(team);
  _scheduleNextQuestion(team);
};
```

Linear flow，3 個 branch (pending ult / correct / wrong) 全部 explicit named helper。

## Test assertions (T28 block, 9 new + 4 refactored)

### Refactored existing assertions (T5, T19, F-03):

- `T5: handleAnswer string fallback` → `_compareAnswer string fallback String()===String()`
- `T5: handleAnswer Array.isArray branch` → `_compareAnswer has Array.isArray branch`
- `T5: handleAnswer number branch` → `_compareAnswer number branch uses Number()`
- `T19: AUDIT6-F01 nulls current` → `_scheduleNextQuestion contains null-out + coop/versus branch`
- `F-03 ordering branch length+JSON` → `_compareAnswer ordering check uses length+JSON`
- `F-03 ordering console.warn` → `_compareAnswer ordering console.warn (expected/actual)`

每個都加 `||` fallback 對應 new helper location，確保將來唔 revert 結構。

### New T28 assertions (R5-1 to R5-8):

- **R5-1**: Game.handleAnswer body 包含 `_scheduleNextQuestion(team);` 結尾
- **R5-2**: _compareAnswer helper defined
- **R5-3**: _handlePendingUlt returns boolean (return true;)
- **R5-4**: _applyCorrectEffects takes prevRagePct
- **R5-5**: _applyWrongEffects **唔** take prevRagePct (negative marker)
- **R5-6**: _recordAnswerStats called after correct/wrong effects
- **R5-7**: _scheduleNextQuestion contains null-out + coop branch
- **R5-8**: _compareAnswer ordering check uses length+JSON (negative against revert)

## Functional smoke test (10/10 pass)

Standalone eval `_compareAnswer` 喺 sandbox:

| Test case | Result |
|---|---|
| number correct (5, 5) | ✓ |
| number wrong (5, 6) | ✓ |
| number string actual (5, '5') → Number coercion | ✓ |
| sym-string correct ('<', '<') | ✓ |
| sym-string wrong ('<', '>') | ✓ |
| ordering correct ([1,2,3], [1,2,3]) | ✓ |
| ordering wrong order ([1,2,3], [3,2,1]) | ✓ |
| ordering wrong length ([1,2,3], [1,2]) | ✓ |
| ordering actual non-array ([1,2,3], 5) | ✓ |
| array non-ordering type ([1,2,3], [1,2,3]) | ✓ |

Console.warn fires correctly on 3 ordering wrong cases (debug aid)。

## Invariants preserved

| Cycle / Fix | Where in new code |
|---|---|
| F-03 ordering strict check | `_compareAnswer` Array branch |
| F-03 ordering console.warn | `_compareAnswer` isOrdering block |
| AUDIT6-F01 re-entrancy null-out | `_scheduleNextQuestion` line 1 |
| AUDIT6-F02 pendingUlt guard | `_handlePendingUlt` first check |
| AUDIT7-F07 lastResumableType cache | `_applyCorrectEffects` after t.correct++ |
| AUDIT7 bossRageThreshold hp tick | `_applyCorrectEffects` prevRagePct comparison |
| AUDIT14 checkMastery ordering | `_recordAnswerStats` after recordAnswer |
| P1-A Snapshot.schedule | 3 places (ult-resolve / answer) |
| P1-B Adaptive.onCorrect/onWrong | _applyCorrect / _applyWrong |
| P1-E-2 resetStreak | _applyWrongEffects end |
| V3.4-AUDIT4 announceToA11y | _applyCorrect / _applyWrong |
| V3.4-UI2 screenShake | _applyWrongEffects |
| V3.4-U2 instantHint | _applyWrongEffects (跟 settings.instantHint) |
| V3.4-F3 streak trigger | _recordAnswerStats end |

## Staged list (等 user commit，獨立 cycle 15 commit)

```
M  index.html                       (R5 refactor: 6 helpers extracted, -578 UTF-8 bytes)
M  tests/regression.js              (+9 T28 + 6 refactored T5/T19/F-03 assertions)
A  .harness/changelogs/2026-07-09-cycle15.md
```

## Commit message 建議

```
Math Guardian v3.5 — audit cycle 15 (R5 Game.handleAnswer refactor + 9 test assertions)

R5: extract 6 module-level helpers from Game.handleAnswer:
  - _compareAnswer(expected, actual, type)        → pure, returns {correct}
  - _handlePendingUlt(team, q, value, correct, btn) → returns boolean
  - _applyCorrectEffects(team, q, btn, prevRagePct) → side-effectful
  - _applyWrongEffects(team, q, btn)               → side-effectful
  - _recordAnswerStats(team, q, correct)            → reaction + Profile + mastery + streak
  - _scheduleNextQuestion(team)                     → null-out + coop/versus routing

handleAnswer body: 149 lines / 4.8 KB → 13 lines / 659 chars orchestrator.

Invariants preserved: F-03 ordering strict + warn, AUDIT6-F01 re-entrancy,
AUDIT7-F07 lastResumableType cache, AUDIT14 checkMastery ordering,
P1-A/B/E + V3.4-AUDIT4 a11y announce + V3.4-U2 instantHint + V3.4-F3 streak.

Test delta: 294 → 303 (+9 T28 + 6 refactored T5/T19/F-03).
File size: -578 UTF-8 bytes (591.0 KB char count).
Smoke test 10/10 standalone _compareAnswer cases pass.
```

## 下一步

- **Commit**: cycle 15 獨立 commit
- **Cycle 16 candidate**: R1 next-up refactor — QuestionGen.make
  (cycle 12 deferred 過 R2/R3，今 stable 咗 adaptive 可以 pick up)