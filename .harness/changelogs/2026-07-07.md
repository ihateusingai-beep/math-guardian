# Math Guardian v3.5 — audit cycle 7 (2026-07-07)

## Cycle 8 — 4 fixes + 16 test assertions (T20)

| Fix | Severity | 一句總結 |
|---|---|---|
| **F-06** | P0 | TTS `speak()` / `cancel()` 加 `_boundaryEpoch` counter；onBoundary 收到 callback 先檢查 epoch，唔 match 就 early return。修咗 Chrome cancel() 後仲 fire 一次 onboundary 嘅 race，會 highlight 已被移除嘅 token spans。 |
| **F-07** | P0 | Visual-type ladder fallback 改 Option C：nextQuestion 揀 type 先 check `_typeSupportsMode(type, mode)`，唔 match 就 3-hop fallback chain：(1) 同 settings 入面 mode-compatible 嘅 type → (2) `lastResumableType[team]` cached last-good → (3) 最終 `add10` numeric。全部 fallback path console.warn + a11y announce 通知老師/screen-reader。對中度 SEN 學生：保留 visual scaffold 唔會 silently 被抽走。 |
| **F-08** | P1 | `announceToA11y` 改 300ms trailing debounce：連續 calls (correct 100ms 後 wrong) 合併成一個 message，避免 screen-reader 堆積兩個 message 對中度學生做成 cognitive overload。 |
| **F-03** | P1 | `Game.handleAnswer` ordering branch 加 `q.type === 'ordering'` strict guard（length check）+ 答錯嗰陣 `console.warn(expected vs got)` debug aid。ordering 模式嘅 array compare 唔可以 silent 通過 length-mismatch。 |

## Metrics

| 項目 | 結果 |
|---|---|
| Tests | **225 / 225 pass** (209 + 16 T20) |
| File size | 559.4 KB / 600 KB |
| Lines | 5481 (index.html) / 547 (regression.js) |
| Diff | ~125 ins / ~12 del |

## Design call confirmed

- **F-07 policy**: Option C (cache last resumable type) — 對中度智障學生最 personalized，但 fallback chain 嚴格 ≤ 3 hops 防無限 loop
- **F-01**: null-out 行徑 commit 喺 56bdb5f，OK 而家咁就夠
- **F-02**: ult review chip 仍 work，已 commit

## Next

- **Cycle 9**: 視乎 user signal。可能 spin (a) Snapshot.resume 之後再加 corrupt-check 防 partial JSON, (b) Question Bank lite P0, (c) 自動推薦 (Adaptive.nextType)
- **R1-R9 refactor**: 仍 pending — 7-item list 喺上一輪 message，等 user deliberate