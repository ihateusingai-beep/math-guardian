# Changelog — 2026-09-11

## audit cycle 37: senior-engineer refactor

**Commit:** `a0f33e7` · **380 pass / 0 fail**

### Changes

| Area | Description |
|------|-------------|
| P2 dead code | 移除 `BOSS_HERO` + `AI_ASSETS` 兩個空 object（cleared in cycle 34，structure殘留）|
| P2 readsites | 5 個 readsites 全部改為 direct SVG sprite fallback（boss select / intro / startGame / endIcon / endBg）|
| P0 cleanup | 移除 line 4787-4788 殘留 P1-R3 comment block |
| P1 logic | `decodeAnswer`: 加 `hex.length % 2 !== 0` guard，防止 odd-length crash |
| P1 logic | `showInstantHint` groupAdd hint: `'=' ❓'` → robust `/\s*=\s*❓\s*$/` regex |
| P1 UI | `_senShowFeedback`: 加 `fb._clearTimer` tracking，快速答題不 overlap |
| Test | T14: instant-hint cap regex limit 2000→3000（showInstantHint grew past old limit）|
| Test | T14/T15: 移除 3 個已死 assertions（AUDIT3-C1 ×2, AUDIT4-M）|
| Test | T15: AUDIT4-C pattern fix `[^;]`→`[^}]` |

### File delta
- `index.html`: 50 insertions, 106 deletions → ~293 KB
- `tests/regression.js`: +2 comment lines, −3 dead assertions

---

## 2026-09-11 — cycle 38: groupAdd same fruit both sides

**Commit:** `1c4f4a5` · **381 pass / 0 fail** (+1 assertion)

| Area | Description |
|------|-------------|
| `groupAdd` generator | 兩邊同一款水果（`f.repeat(g1)` + `f.repeat(g2)`）而非隨機不同款 |
| Bank seed `groupAdd` | 6 條 fixtures 全部改為同一款水果 |
| Test | `EASY-26b` lock-in assertion 防止退化 |

### No regression
Core flow unchanged. All 6 layout renderers, `QuestionRenderer` dispatch, `_compareAnswer` triple-path, `Game.handleAnswer`, `_scheduleNextQuestion`, TTS cancel, boss threshold detection — all verified intact.
